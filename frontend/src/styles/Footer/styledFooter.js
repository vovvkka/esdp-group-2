import styled from "@emotion/styled";
import {
    Typography,
} from "@mui/material";

export const FooterTitle = styled(Typography)(() => ({
    textTransform: "uppercase",
    marginBottom: "1em",
}));

