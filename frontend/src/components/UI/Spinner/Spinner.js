import React from 'react';
import './Spiner.css';

const Spinner = () => {
    return (
        <div id="preloader">
            <span className="loader"></span>
        </div>
    );
};

export default Spinner;